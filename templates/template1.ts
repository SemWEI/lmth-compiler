class ${PLACEHOLDER_NAME} {
    private type: ${PLACEHOLDER_TYPE};
    constructor(type: ${PLACEHOLDER_TYPE}) {
        this.type = type;
    }
    public getType(): ${PLACEHOLDER_TYPE} {
        return this.type;
    }
}