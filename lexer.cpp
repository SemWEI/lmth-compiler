#include "lexer.h"

std::vector<Token> Lexer::tokenize(const std::string &source) {
    std::vector<Token> tokens;

    // Implement simple lexing logic for a toy language
    // For example, identify keywords, identifiers, numbers, etc.

    return tokens;
}